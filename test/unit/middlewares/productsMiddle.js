const { expect } = require('chai');
const sinon = require('sinon');
const connection = require('../../../models/connection');

const { getAllProductsMiddleware } = require('../../../middlewares/productsMiddle');

describe('Products Middlewares', () => {
  describe('getAllProductsMiddleware', () => {
    describe('quando a tabela `products` tiver dados', () => {
      it('com sucesso', () => {

      });
    })
  });
});
